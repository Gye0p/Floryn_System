{% extends 'base.html.twig' %}

{% block title %}Add Customer - Floryn Garden{% endblock %}
{% block page_title %}Add New Customer{% endblock %}

{% block body %}
<div class="page-wrapper px-4 sm:px-6 lg:px-8">
  <nav class="breadcrumb">
    <a href="{{ path('admin_dashboard') }}"><i class="fas fa-home"></i></a>
    <span class="separator">/</span>
    <a href="{{ path('app_customer_index') }}">Customers</a>
    <span class="separator">/</span>
    <span class="current">New Customer</span>
  </nav>

  <div class="page-header">
    <div class="page-header-info">
      <div class="avatar md" style="background:linear-gradient(135deg,#7c5cbf,#9b59b6);"><i class="fas fa-user-plus" style="font-size:18px;"></i></div>
      <div>
        <h1>Add New Customer</h1>
        <p class="subtitle">Enter customer details to add them to your database.</p>
      </div>
    </div>
  </div>

  <div style="max-width:900px;">
    {{ include('customer/_form.html.twig', {button_label: 'Create Customer'}) }}
  </div>
</div>
{% endblock %}