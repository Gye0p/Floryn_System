{% extends 'base.html.twig' %}

{% block title %}Add Supplier - Floryn Garden{% endblock %}
{% block page_title %}Add New Supplier{% endblock %}

{% block body %}
<div class="page-wrapper px-4 sm:px-6 lg:px-8">
  <nav class="breadcrumb">
    <a href="{{ path('admin_dashboard') }}"><i class="fas fa-home"></i></a>
    <span class="separator">/</span>
    <a href="{{ path('app_supplier_index') }}">Suppliers</a>
    <span class="separator">/</span>
    <span class="current">New Supplier</span>
  </nav>

  <div class="page-header">
    <div class="page-header-info">
      <div class="avatar md" style="background:linear-gradient(135deg,#3b82f6,#1d4ed8);"><i class="fas fa-truck-loading" style="font-size:18px;"></i></div>
      <div>
        <h1>Add New Supplier</h1>
        <p class="subtitle">Enter supplier details to add them to your network.</p>
      </div>
    </div>
  </div>

  <div style="max-width:900px;">
    {{ include('supplier/_form.html.twig', {button_label: 'Create Supplier'}) }}
  </div>
</div>
{% endblock %}