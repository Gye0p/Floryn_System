{# Standard session CSRF hidden field (no stateless JS required) #}
{% block csrf_token_widget %}
<input type="hidden"
       id="{{ id }}"
       name="{{ full_name }}"
       value="{{ value }}"
/>
{% endblock %}
