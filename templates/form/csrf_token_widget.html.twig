{# Required for stateless CSRF (token_id: submit) with Turbo — see assets/controllers/csrf_protection_controller.js #}
{% block csrf_token_widget %}
<input type="hidden"
       id="{{ id }}"
       name="{{ full_name }}"
       data-controller="csrf-protection"
       value="{{ value }}"
/>
{% endblock %}
