<form method="post" action="{{ path('app_notification_log_delete', {'id': notification_log.id}) }}" onsubmit="return confirm('Are you sure you want to delete this notification log? This action cannot be undone.');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ notification_log.id) }}">
    <button type="submit"
            class="w-full inline-flex items-center justify-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 transition-colors duration-200">
        <i class="fas fa-trash mr-2"></i>
        Delete Notification
    </button>
</form>
