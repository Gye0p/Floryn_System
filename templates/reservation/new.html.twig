{% extends 'base.html.twig' %}

{% block title %}New Reservation - Floryn Garden{% endblock %}
{% block page_title %}Create New Reservation{% endblock %}

{% block body %}
<div class="page-wrapper px-4 sm:px-6 lg:px-8">
  <nav class="breadcrumb">
    <a href="{{ path('admin_dashboard') }}"><i class="fas fa-home"></i></a>
    <span class="separator">/</span>
    <a href="{{ path('app_reservation_index') }}">Reservations</a>
    <span class="separator">/</span>
    <span class="current">New Reservation</span>
  </nav>

  <div class="page-header">
    <div class="page-header-info">
      <div class="avatar md" style="background:linear-gradient(135deg,#9b59b6,#8e44ad);"><i class="fas fa-calendar-plus" style="font-size:18px;"></i></div>
      <div>
        <h1>Create New Reservation</h1>
        <p class="subtitle">Enter reservation details to create a new flower order.</p>
      </div>
    </div>
  </div>

  <div style="max-width:1000px;">
    {{ include('reservation/_form.html.twig', {button_label: 'Create Reservation'}) }}
  </div>
</div>
{% endblock %}