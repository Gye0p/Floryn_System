{% extends 'base.html.twig' %}

{% block title %}Top Sellers - Floryn Garden{% endblock %}

{% block body %}
<div class="max-w-7xl mx-auto">
  {# Header #}
  <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-8 gap-4">
    <div>
      <h1 class="text-3xl font-bold text-gray-900 flex items-center">
        <i class="fas fa-trophy text-yellow-500 mr-3"></i>
        Top Selling Flowers
      </h1>
      <p class="mt-1 text-gray-600">Most popular flowers by units sold</p>
    </div>
    <a href="{{ path('app_reports_index') }}" class="inline-flex items-center px-4 py-2 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 transition text-sm">
      <i class="fas fa-arrow-left mr-2"></i> Back
    </a>
  </div>

  {# Top Sellers Table #}
  <div class="bg-white rounded-xl shadow-md overflow-hidden">
    <div class="p-6 border-b border-gray-200">
      <h2 class="text-lg font-semibold text-gray-900"><i class="fas fa-star mr-2 text-yellow-500"></i>Flower Rankings</h2>
    </div>
    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-gray-200" id="topSellersTable">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Rank</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Flower</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Category</th>
            <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase">Units Sold</th>
            <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase">Total Revenue</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          {% for i, seller in top_sellers %}
          <tr class="hover:bg-gray-50">
            <td class="px-6 py-4 text-sm">
              {% if i < 3 %}
                <span class="inline-flex items-center justify-center w-8 h-8 rounded-full text-sm font-bold {% if i == 0 %}bg-yellow-100 text-yellow-800{% elseif i == 1 %}bg-gray-200 text-gray-700{% else %}bg-orange-100 text-orange-800{% endif %}">
                  {% if i == 0 %}<i class="fas fa-crown"></i>{% else %}{{ i + 1 }}{% endif %}
                </span>
              {% else %}
                <span class="text-gray-500 ml-2">{{ i + 1 }}</span>
              {% endif %}
            </td>
            <td class="px-6 py-4 text-sm font-medium text-gray-900">{{ seller.name }}</td>
            <td class="px-6 py-4 text-sm text-gray-700">{{ seller.category }}</td>
            <td class="px-6 py-4 text-sm text-gray-900 text-right font-semibold">{{ seller.totalSold }}</td>
            <td class="px-6 py-4 text-sm text-green-600 text-right font-semibold">₱{{ seller.totalRevenue|number_format(2) }}</td>
          </tr>
          {% else %}
          <tr>
            <td colspan="5" class="px-6 py-12 text-center text-gray-500">No sales data available yet.</td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </div>
</div>
{% endblock %}
