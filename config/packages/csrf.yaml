# Session-based CSRF for admin forms (reliable with Turbo).
# Stateless CSRF only for logout; login uses authenticate token via security.yaml.
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - logout
